# Project.CD 咖啡甜點

無門市、在家現做的咖啡甜點電商官網。純 HTML/CSS/JS,零建置工具(no npm/bundler),所有第三方套件(Firebase、GSAP、Font Awesome 等)一律用 CDN `<script>` 標籤引入,不要改成 ESM `type="module"`——這是刻意的架構選擇,不是技術債。

## 設計系統(必看)

正式的設計系統文件是一份已發布的 Claude Artifact:**咖啡甜點設計系統**
https://claude.ai/code/artifact/16b3fa05-5402-4711-b3a6-69e6c9f46649

做任何新的視覺/設計決策前,先用 WebFetch 讀取這份文件確認目前的數值,不要單憑記憶假設(文件本身會隨時間更新)。文件涵蓋:設計方向與目標(Awwwards 標準、純黑白配色、iOS 視覺語言)、色彩 token、字體、4px 間距系統、視覺/聽覺/觸覺/互動四感設計原則、元件模式、響應式斷點。如果做出了文件裡還沒收錄的新模式,要回頭更新文件(編輯來源檔案、重新發布到同一個 artifact 網址),讓它保持唯一真實來源。不要在沒跟使用者討論過的情況下,自己發明文件規範以外的顏色/字級/間距/動效曲線。

## 設計工作標準

做任何 UI/UX 或視覺設計工作時,要當一個專業 UI/UX 專家,不是憑感覺調數值——主動對照公認標準:
- **WCAG 無障礙**:觸控目標尺寸、色彩對比
- **iOS Human Interface Guidelines**:44×44pt 最小觸控目標、半透明「Materials」用固定的 Light/Dark 變體(依情境切換 class,不要即時偵測背景色去動態運算,那是這個專案踩過的坑,見下方導覽列架構說明)、髮絲級邊框用接近 0.5px 的透明度、玻璃材質上的圖示/文字顏色用 90-92% 而非純白/純黑
- **4px 間距網格**:`--sp-1` (4px) 到 `--sp-16` (64px),新的 gap/padding 數值要對齊這個階梯

提出新的視覺處理方式時,講清楚它對應哪個 HIG 概念;如果某個做法會偏離 HIG(例如即時自適應模糊),要明確提出來讓使用者自己決定,不要默默做掉。

## 導覽列架構(重要,避免重蹈覆轍)

導覽列(`.nav-links` 首頁/品牌/產品/商城膠囊、`.nav-icon-btn`/`.nav-icon-link` 會員/聯繫/購物車圓形圖示)是**液態玻璃**,但材質固定成 Light/Dark 兩組,**不是即時運算背景色**——依 `.navbar.on-dark` 這個 class 切換用哪一組,跟 LOGO 文字共用同一套判斷時機。

背景色的判斷歷史,是這裡最容易重蹈覆轍的坑:早期版本讓導覽列圖示的顏色即時跟著背景深淺切換,用兩套各自獨立的偵測邏輯——一個是 `scroll` 事件搭配寫死的捲動距離門檻,一個是 `IntersectionObserver` 驅動的選單高亮,兩套邏輯時機對不上,造成使用者能明顯感覺到的顏色閃爍/不同步。當時的解法是把導覽列圖示全部改成永遠不透明的白色浮島,讓「要不要判斷背景」這個問題直接不存在。後來（2026-09）液態玻璃材質站上全站鋪開,使用者要求導覽列也要有玻璃的立體感,於是改回會判斷背景深淺——但只透過**唯一**、既有的 `navObserver` 機制切換 class,不是重新做即時運算，才沒有重蹈當年閃爍的覆轍。

**LOGO 文字**(`.navbar.on-dark` 切換 `.logo-text` 顏色)跟導覽列圖示現在用的是同一個判斷來源:`js/script.js` 裡的 `navObserver`(`IntersectionObserver`,`rootMargin: '-30% 0px -60% 0px'`),跟選單高亮共用同一個觀察者、同一個時機來源——**不要**再另外加一個獨立的 scroll 事件監聽器去做類似的事,那正是當初閃爍問題的根源。

液態玻璃的固定配方(Light/Dark 兩組,含邊框/陰影/角落高光數值)見設計系統文件 05 節「元件模式」;幫其他元件做玻璃材質時,直接照抄這個模式:準備好 Light/Dark 兩種固定材質,依所在區塊套用哪一種,而不是即時運算背景色。

## 已知會踩到的坑

- **`--disable-gpu` 加上無頭瀏覽器截圖**:畫面會變透明,截圖驗證用 headless 時不要加這個參數。
- **`window.scrollTo()` / `scrollIntoView()` 在無頭瀏覽器裡不可靠**:這個頁面是 `scroll-snap-type: y mandatory`,無頭環境下用程式觸發捲動常常實際沒有真的捲動。要驗證特定區塊的樣式,改用「暫時隱藏它上面的區塊讓目標區塊變成第一個」或是注入診斷用的 `getComputedStyle()`/`getBoundingClientRect()` 覆蓋層截圖,不要硬幹捲動模擬。
- **無頭瀏覽器 `--window-size` 在這台機器上可能有 DPI 縮放誤差**:實際回報的 `window.innerWidth` 可能跟指定的寬度對不上,測試手機版排版時數學算過一遍會比硬要截圖驗證準確。
- **測試視窗寬度一定要避開手機斷點誤判**:斷點在 1024px,桌機版測試要開到明顯大於 1024px(例如 1440px),不要開 1000px 這種卡在斷點邊緣的寬度。
